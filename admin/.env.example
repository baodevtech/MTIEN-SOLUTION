# ========================================
# Admin CMS Environment Variables
# ========================================

# Data directory path (default: ../data when running inside main project)
# For standalone deployment, set to: ./data
# DATA_DIR=./data
